# TLS消息捕获结果说明

## ✅ 测试成功完成！

已成功运行TLS握手并捕获了所有消息。

## 捕获的消息

测试运行时间：2026-01-31 13:42:45
捕获目录：`tls_capture_20260131_134244/`

### 消息列表

共捕获了 **8条消息**：

1. **Message #1: Client->Server**
   - ContentType: 0x16 (Handshake)
   - HandshakeType: 0x01 (ClientHello)
   - Length: 297 bytes
   - 包含：客户端版本、随机数、加密套件列表、扩展等

2. **Message #2: Server->Client**
   - ContentType: 0x16 (Handshake)
   - HandshakeType: 0x02 (ServerHello)
   - Length: 1359 bytes
   - 包含：服务器版本、随机数、选中的加密套件等
   - 注意：此消息可能包含多个握手消息（ServerHello + Certificate + ServerKeyExchange等）

3. **Message #3: Client->Server**
   - ContentType: 0x14 (ChangeCipherSpec)
   - 客户端通知服务器开始使用加密

4. **Message #4-8: ApplicationData**
   - ContentType: 0x17 (ApplicationData)
   - 这些是加密后的应用数据（握手完成后的数据传输）

## 文件位置

所有捕获的消息保存在：
```
tls_capture_20260131_134244/tls_all_messages.txt
```

文件大小：37KB
总行数：579行

## 消息格式说明

每条消息包含：

1. **消息头部信息**：
   - 消息编号和方向
   - 时间戳
   - 消息长度

2. **记录层解析**：
   - ContentType（内容类型）
   - Version（TLS版本）
   - Length（记录长度）

3. **握手层解析**（如果是握手消息）：
   - HandshakeType（握手消息类型）
   - Length（握手消息长度）

4. **完整的十六进制转储**：
   - 每行16字节
   - 左侧：十六进制表示
   - 右侧：ASCII字符（可打印字符）

## 查看消息

### 查看所有消息：
```bash
cat tls_capture_20260131_134244/tls_all_messages.txt
```

### 查看特定消息类型：
```bash
# 查看ClientHello
grep -A 50 "ClientHello" tls_capture_20260131_134244/tls_all_messages.txt

# 查看ServerHello
grep -A 50 "ServerHello" tls_capture_20260131_134244/tls_all_messages.txt

# 查看所有握手消息
grep "HandshakeType:" tls_capture_20260131_134244/tls_all_messages.txt
```

### 统计信息：
```bash
# 消息总数
grep -c "^Message #" tls_capture_20260131_134244/tls_all_messages.txt

# 消息类型统计
grep "HandshakeType:" tls_capture_20260131_134244/tls_all_messages.txt | sort | uniq -c
```

## 重要说明

1. **消息格式**：所有消息都是**原始网络传输格式**，即第三方（网络抓包者）看到的格式

2. **加密状态**：
   - 握手阶段的消息（ClientHello, ServerHello等）是**明文**传输
   - ApplicationData消息是**加密**的（显示为加密数据）

3. **字节序**：所有多字节字段使用**大端序**（网络字节序）

4. **固定字段**：
   - 记录层头部：5字节
   - 握手层头部：4字节
   - 消息体：可变长度

## 再次运行测试

如果需要重新捕获消息：

```bash
cd /home/hosen/mbedtls
./run_full_test.sh
```

每次运行都会创建新的时间戳目录，不会覆盖之前的捕获结果。

## 下一步分析

您现在可以：
1. 分析每条消息的详细格式
2. 查看证书内容（在ServerHello消息中）
3. 验证握手流程
4. 检查加密套件协商过程
